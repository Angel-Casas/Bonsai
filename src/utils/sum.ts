/**
 * Simple utility function for unit test demonstration.
 * This will be replaced with actual utilities in future milestones.
 */
export function sum(a: number, b: number): number {
  return a + b
}
